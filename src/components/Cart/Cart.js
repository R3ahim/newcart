import React from 'react';
import './Cart.css'
const Cart = (props) => {

 
    }



    const tax = parseFloat((total * 0.1).toFixed(2));
    const grandTotal = total + shipping + tax;
    return (
        <div  className='cart'>
           
        </div>
    );
};


export default Cart;